# date 类型支持实现计划

> **For agentic workers:** REQUIRED SKILL: Use planning-with-files to implement this plan task-by-task.

**Goal:** 为 YAML 模板类型系统新增 `date` 类型，支持 `YYYY-MM-DD HH:MM:SS` 格式校验

**Architecture:** 在 `validateType` switch 新增 `case "date"` 分支（使用 `time.Parse` 校验格式和日期合法性），在 `printTemplateTo` 新增 date 类型 note 提示格式，更新文档

**Tech Stack:** Go, time.Parse, TDD

**Spec:** `docs/superpowers/specs/2026-04-01-date-type-design.md`

---

### Task 1: date 类型校验

**Files:**
- Test: `internal/cmdgen/validate_test.go`
- Modify: `internal/cmdgen/validate.go:3-9` (import), `internal/cmdgen/validate.go:103` (case 前)

- [ ] **Step 1: 写 date 类型校验的失败测试**

在 `validate_test.go` 的 `TestValidateType_Nil` 测试后（第 83 行后）追加：

```go
func TestValidateType_Date(t *testing.T) {
	tests := []struct {
		name    string
		value   interface{}
		wantErr bool
	}{
		{"valid datetime", "2026-03-31 17:11:56", false},
		{"valid boundary start", "2026-01-01 00:00:00", false},
		{"valid boundary end", "2026-12-31 23:59:59", false},
		{"missing time part", "2026-03-31", true},
		{"wrong separator", "2026/03/31 17:11:56", true},
		{"invalid date feb 30", "2026-02-30 12:00:00", true},
		{"completely invalid", "not-a-date", true},
		{"non-string type", float64(20260331), true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := validateType("delayTime", tt.value, "date")
			if (err != nil) != tt.wantErr {
				t.Errorf("validateType() error = %v, wantErr %v", err, tt.wantErr)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /home/childelins/code/ckjr-cli && go test ./internal/cmdgen/ -run TestValidateType_Date -v`
Expected: FAIL — date 进入 default 分支返回 "未知类型"

- [ ] **Step 3: 实现 date 类型校验**

在 `validate.go` import 中添加 `"time"`（第 4-9 行）：

```go
import (
	"fmt"
	"math"
	"regexp"
	"time"

	"github.com/childelins/ckjr-cli/internal/router"
)
```

在 `validateType` 函数的 `default:` 前（第 103 行前）插入：

```go
	case "date":
		str, ok := value.(string)
		if !ok {
			return &FieldValidationError{
				Field:   fieldName,
				Message: fmt.Sprintf("类型应为 date（字符串格式），实际为 %T", value),
			}
		}
		const dateLayout = "2006-01-02 15:04:05"
		if _, err := time.Parse(dateLayout, str); err != nil {
			return &FieldValidationError{
				Field:   fieldName,
				Message: fmt.Sprintf("日期格式应为 YYYY-MM-DD HH:MM:SS，实际值 %q 无效: %s", str, err.Error()),
			}
		}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /home/childelins/code/ckjr-cli && go test ./internal/cmdgen/ -run TestValidateType_Date -v`
Expected: PASS

- [ ] **Step 5: 写错误信息格式断言测试**

在 `TestValidateType_Date` 后追加：

```go
func TestValidateType_Date_ErrorMessage(t *testing.T) {
	// 非字符串应提示期望类型
	err := validateType("delayTime", float64(123), "date")
	if err == nil || !strings.Contains(err.Error(), "date（字符串格式）") {
		t.Errorf("non-string error should mention date type, got: %v", err)
	}

	// 格式错误应提示期望格式
	err = validateType("delayTime", "2026-03-31", "date")
	if err == nil || !strings.Contains(err.Error(), "YYYY-MM-DD HH:MM:SS") {
		t.Errorf("format error should show expected format, got: %v", err)
	}
}
```

注意：`validate_test.go` 需要在 import 中添加 `"strings"`。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd /home/childelins/code/ckjr-cli && go test ./internal/cmdgen/ -run TestValidateType_Date -v`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add internal/cmdgen/validate.go internal/cmdgen/validate_test.go
git commit -m "feat(validate): add date type validation with time.Parse"
```

### Task 2: --template 输出 date note

**Files:**
- Test: `internal/cmdgen/cmdgen_test.go`
- Modify: `internal/cmdgen/cmdgen.go:160` (path note 判断后)

- [ ] **Step 1: 写 date note 的失败测试**

在 `cmdgen_test.go` 的 `TestPrintTemplate_PathFieldNote` 测试后追加：

```go
func TestPrintTemplate_DateFieldNote(t *testing.T) {
	template := map[string]router.Field{
		"delayTime": {
			Description: "定时上架时间",
			Required:    false,
			Type:        "date",
		},
		"name": {
			Description: "名称",
			Required:    true,
			Type:        "string",
		},
	}

	var buf bytes.Buffer
	printTemplateTo(&buf, template, nil)

	var outer map[string]interface{}
	if err := json.Unmarshal(buf.Bytes(), &outer); err != nil {
		t.Fatalf("JSON parse error: %v", err)
	}
	result := outer["request"].(map[string]interface{})

	// date 类型应有 note
	dateEntry := result["delayTime"].(map[string]interface{})
	note, ok := dateEntry["note"]
	if !ok {
		t.Fatal("delayTime (type=date) should have note field")
	}
	if note != "日期格式: YYYY-MM-DD HH:MM:SS" {
		t.Errorf("note = %q, want %q", note, "日期格式: YYYY-MM-DD HH:MM:SS")
	}

	// 非 date 类型不应因此多出 note
	nameEntry := result["name"].(map[string]interface{})
	if _, exists := nameEntry["note"]; exists {
		t.Error("name (type=string) should not have note field")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /home/childelins/code/ckjr-cli && go test ./internal/cmdgen/ -run TestPrintTemplate_DateFieldNote -v`
Expected: FAIL — date 类型无 note 字段

- [ ] **Step 3: 实现 date note**

在 `cmdgen.go` 的 `printTemplateTo` 函数中，path note 判断（第 158-160 行）后追加：

```go
		if t == "date" {
			entry["note"] = "日期格式: YYYY-MM-DD HH:MM:SS"
		}
```

- [ ] **Step 4: 运行全部测试确认通过**

Run: `cd /home/childelins/code/ckjr-cli && go test ./internal/cmdgen/ -v`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add internal/cmdgen/cmdgen.go internal/cmdgen/cmdgen_test.go
git commit -m "feat(template): add date type note in --template output"
```

### Task 3: 更新文档

**Files:**
- Modify: `wiki/core-concepts.md:67` (type 表格)
- Modify: `wiki/extending.md:98` (type 属性说明)

- [ ] **Step 1: 更新 core-concepts.md 类型校验表**

在 `wiki/core-concepts.md` 第 67 行（`path` 行）后添加：

```markdown
| `date` | 日期时间 | JSON string（格式 `YYYY-MM-DD HH:MM:SS`） |
```

- [ ] **Step 2: 更新 extending.md template 属性表**

在 `wiki/extending.md` 第 98 行，将 type 描述从：

```
| `type` | string | 否 | 类型：string/int/float/bool/array/path。`path` 用于替换 URL 路径占位符 `{xxx}` |
```

改为：

```
| `type` | string | 否 | 类型：string/int/float/bool/array/path/date。`path` 用于替换 URL 路径占位符 `{xxx}`，`date` 格式为 `YYYY-MM-DD HH:MM:SS` |
```

- [ ] **Step 3: 提交**

```bash
git add wiki/core-concepts.md wiki/extending.md
git commit -m "docs: add date type to type system documentation"
```

Q1: test文件分散到所有地方，最好放在专门的测试目录下

Q2: ./ckjr agent list '{"page":1,"limit":20}'
{"error":"解析响应失败: invalid character '\u003c' looking for beginning of value"}

Q3: 缺少日志记录
我应该能看到执行的日志情况（比如 client 发起请求的信息），最好日志里能带上 requestId。通过 requestId 能拿到本地请求的信息。

Q4: agent.yaml 文件缺少参数类型，以及示例。
参数类型可以默认都是 string，有需要再指定。示例看需不需要，有些特殊参数要给示例参考。
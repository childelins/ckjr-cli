Q1: 将 cmd/routes, cmd/workflows 下的 yaml 文件放到根目录下的 config/routes, config/workflows。

Q2: 优化下整体目录结构，比如 cmd 散列了太多的文件，最好能放到二级目录

Q3: 我希望在本地构建多平台编译产物到 bin 目录下，然后再通过某个方式上传到 github 公开库 https://github.com/childelins/ckjr-cli 的 release 里。
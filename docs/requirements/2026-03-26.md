Q1: 生成的 cli 名称不用 ckjr，改为 ckjr-cli。公司名称是“创客匠人”。

技能文件里的调用方式都得改下，还有README.md 文件等等。

Q2: 有一个需求，我想通过给定一个 curl 地址，快速生成 yaml 文件（可以是追加到已有文件或者新建）。明确的参数直接生成，如果描述等字段比较模糊后面再由我人工补充，但需要先占位。

Q3: log 日志记录不够详细，我需要详细记录发起 http 请求的 body, 还有 response。在日志里能详细看到。

Q4: ckjr-cli route import 是内部命令，我不希望暴露给 AI 调用。

Q5: skills.md 文件技能名称也改为 ckjr-cli。不叫 ckjr-agent。